{
  "name": "ghost-video-extension",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "pack": "cd extension && zip -r ../ghost-extension.zip ."
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "author": "siega52",
  "description": "Browser extension that adds ghost effects to videos"
}