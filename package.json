{
  "name": "ghost-extension",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:extension": "vite build && node scripts/prepare-extension.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.2.2"
  }
}
